# All: check if all elements satisfy predicate
# Input: n (tests range 1 to n)
# Postcondition: true iff all elements > 0
# Property: all p [] = true
# Property: all p (x:xs) = p x && all p xs

╭─ isPositive : I64 → Bool
╰─ ₀ > 0

╭─ main : I64 → Bool
╰─ let arr = range 1 (₀ + 1) in
   Σ (arr ↦ (λ→ toInt (isPositive ₀))) == len arr
