# Ackermann function - classic recursion test
# Usage: goth ackermann.goth <m> <n>
# Example: goth ackermann.goth 3 4  →  125
#
# A(0, n) = n + 1
# A(m, 0) = A(m-1, 1)
# A(m, n) = A(m-1, A(m, n-1))
#
# Warning: grows extremely fast! A(4,2) has 19729 digits

╭─ ack : I64 → I64 → I64
╰─ if ₁ = 0 then ₀ + 1
   else if ₀ = 0 then ack (₁ - 1) 1
   else ack (₁ - 1) (ack ₁ (₀ - 1))

╭─ main : I64 → I64 → I64
╰─ ack ₁ ₀
